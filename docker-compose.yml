version: "3.9"

services:
  pipeline:
    build: .
    container_name: urban-analytics-pipeline
    volumes:
      - ./data:/app/data
      - ./docs:/app/docs
    environment:
      - PYTHONUNBUFFERED=1